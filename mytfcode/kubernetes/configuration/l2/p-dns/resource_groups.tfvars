resource_groups = {
  dns-pri = {
    name        = "p-dns-pri"
    region      = "region1"
    description = "Resource Group for Private DNS Records"
  }
  dns-pub = {
    name        = "p-dns-pub"
    region      = "region1"
    description = "Resource Group for Public DNS Records"
  }

}